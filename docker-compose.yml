version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ehtms_user
      POSTGRES_PASSWORD: ehtms_password_123
      POSTGRES_DB: ehtms_db
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
